# Подключается в http {} (см. include /etc/nginx/conf.d/*.conf).
# Без map Open WebUI ломает обычный HTTP, если всегда Connection: upgrade — 502 / обрыв WebSocket.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
