# Проверка по ТЗ: порядок, промпты, критерии

Как устроено в целом: **браузер → Open WebUI** (`:3000`) → **GPTHub Gateway** (`/v1/*`, в compose с хоста `:8081`) → **MWS GPT**. Режим **`gpthub-auto`** включает роутер gena (перехваты: презентация, картинка, deep research, музыка и т.д.) и эвристики выбора модели. Ручной выбор модели из списка — шлюз **не подменяет** `model`.

Перед проверками: `docker compose ps` (шлюз **healthy**), в `.env` актуальный **`MWS_API_KEY`**, для картинок/PPTX в чате — **`GPTHUB_PUBLIC_BASE_URL`** (локально часто `http://127.0.0.1:8081`). Модель в UI: **`gpthub-auto`**, кроме шага про ручной выбор.

Отмечайте: **ОК** / **не ОК** и кратко что увидели (ошибка, пустой ответ, нет картинки).

---

## Этап 0 — инфраструктура (без чата)

| # | Проверка | Как | ОК если |
|---|----------|-----|---------|
| 0.1 | Шлюз жив | В браузере или `curl http://127.0.0.1:8081/health` | `{"status":"ok"}` |
| 0.2 | Список моделей | В UI видны модели; или `curl -H "Authorization: Bearer $MWS_API_KEY" http://127.0.0.1:8081/v1/models` | Есть `gpthub-auto` и ваши id, нет 502 |

---

## Этап 1 — текстовый чат (ТЗ №1, №10)

**Модель:** `gpthub-auto`.

| # | Промпт (скопировать) | ОК если |
|---|----------------------|---------|
| 1.1 | `В двух предложениях объясни, что такое HTTP-код 404.` | Осмысленный ответ на русском, без ошибки 5xx |
| 1.2 | `Say in one sentence what a prime number is.` | Ответ на **английском** (проверка правила «язык пользователя») |

---

## Этап 2 — маркер роутера (опционально, ТЗ отладка)

**Условие:** `GPTHUB_ROUTER_DEBUG=true` (часто по умолчанию).

| # | Промпт | ОК если |
|---|--------|---------|
| 2.1 | `Привет! Как дела?` | В system/ответе видно что-то вроде `[GPTHub route:` или маршрут понятен по логам |

---

## Этап 3 — VLM, картинка в сообщении (ТЗ №5)

**Модель:** `gpthub-auto`. Прикрепите **один** скриншот или фото (небольшой файл).

| # | Промпт | ОК если |
|---|--------|---------|
| 3.1 | `Что изображено на картинке? Ответь кратко по пунктам.` | Описание соответствует изображению, не общая отписка |

---

## Этап 4 — генерация изображения в чате (ТЗ №3)

**Модель:** `gpthub-auto`. **Не** включайте стрим, если проверяете edge-case музыки отдельно.

| # | Промпт | ОК если |
|---|--------|---------|
| 4.1 | `Нарисуй простую иконку: синий круг с белой буквой G внутри, плоский стиль.` | В ответе **видна картинка** (markdown `![](...)`) по URL, открывается с хоста |
| 4.2 | `Draw a simple red apple on white background, flat icon.` | Картинка есть; проверка EN-запроса к цепочке промптов |

---

## Этап 5 — поиск в интернете (ТЗ №7)

| # | Промпт | ОК если |
|---|--------|---------|
| 5.1 | `Найди в интернете последние новости про запуск космического телескопа за последний год. Дай 2–3 ссылки или факта с указанием источника.` | В ответе есть **актуальные** факты/ссылки, не выдуманные даты |

---

## Этап 6 — веб по ссылке (ТЗ №8)

Подставьте **реальную** HTTPS-страницу (статья, документация).

| # | Промпт | ОК если |
|---|--------|---------|
| 6.1 | `Прочитай https://example.com и скажи, что написано в заголовке страницы.` | Соответствует реальному содержимому (example.com — предсказуемо) |

*Для проверки осмысленности возьмите ссылку на известную статью и спросите «о чём главная мысль в двух предложениях».*

---

## Этап 7 — RAG по длинному тексту (ТЗ №6)

В **одном** сообщении вставьте абзац на 300–800 слов (можно сгенерировать отдельно) и спросите:

| # | Промпт | ОК если |
|---|--------|---------|
| 7.1 | *(вставка текста)* `По тексту выше: какие три факта названы явно? Только из текста.` | Ответ опирается на **вставленный** текст, не на общие знания |

---

## Этап 8 — долговременная память (ТЗ №9)

**Один тред**, модель `gpthub-auto`.

| # | Промпт | ОК если |
|---|--------|---------|
| 8.1 | `Запомни: мой любимый язык программирования — Python.` | Ответ подтверждает / не ошибка |
| 8.2 | *(новое сообщение в том же чате)* `Какой язык программирования я назвал любимым?` | Ответ **Python** (или явно из памяти) |

---

## Этап 9 — код-роутинг (ТЗ №10)

| # | Промпт | ОК если |
|---|--------|---------|
| 9.1 | `Напиши на Python функцию is_prime(n), которая возвращает True для простых чисел.` | Код на Python, в логах/маршруте ожидаем ветку code (или адекватный ответ) |

---

## Этап 10 — ручная модель (ТЗ №11)

Переключите модель на **конкретный** id из списка (не `gpthub-auto`), например `llama-3.1-8b-instruct`.

| # | Промпт | ОК если |
|---|--------|---------|
| 10.1 | `Ответь одним словом: тест` | Ответ приходит, модель **не** подменена на авто-другую (проверка по отсутствию перехвата презентации и т.д.) |

---

## Этап 11 — Deep Research (ТЗ №13, доп.)

| # | Промпт | ОК если |
|---|--------|---------|
| 11.1 | `Сделай глубокое исследование темы: влияние длины контекста LLM на качество RAG. Структурируй: введение, 3 тезиса, вывод.` | Длинный стрим, есть разделы/тезисы, не одна фраза |

---

## Этап 12 — Презентация PPTX (ТЗ №14, доп.)

| # | Промпт | ОК если |
|---|--------|---------|
| 12.1 | `Сделай короткую презентацию на 5 слайдов про плюсы удалённой работы, без лишней воды.` | Файл PPTX или ссылки скачивания, открывается; при желании проверьте редактор по ссылке из ответа |

---

## Этап 13 — голос / STT (ТЗ №2, №4)

| # | Действие | ОК если |
|---|----------|---------|
| 13.1 | Запишите **русскую** фразу в микрофон: «Сегодня хорошая погода». | Транскрипт близок к сказанному |
| 13.2 | Запишите **английскую** фразу: «The quick brown fox jumps over the lazy dog.» | Транскрипт на английском разборчив (при пустом `GPTHUB_ASR_DEFAULT_LANGUAGE` — авто) |

---

## Этап 14 — TTS (ТЗ №15)

Включите озвучку ответа в UI, если есть.

| # | Действие | ОК если |
|---|----------|---------|
| 14.1 | Короткий ответ → «прочитать» / TTS | Слышен звук, не ошибка API |

---

## Этап 15 — демо MP3 (ТЗ №15 доп., только без стрима)

Шлюз перехватывает запрос только при **`stream: false`** и модели **`gpthub-auto`**. Если Open WebUI всегда шлёт стрим — проверьте через `curl` к `/v1/chat/completions` с `"stream": false` или настройки UI.

| # | Промпт | ОК если |
|---|--------|---------|
| 15.1 | `Сгенерируй короткую мелодию в духе детской песенки, запиши в mp3.` | В ответе markdown-ссылка на `/static/music/...` (через `GPTHUB_PUBLIC_BASE_URL`), файл открывается/скачивается |

---

## Этап 16 — Markdown / код (ТЗ №12)

| # | Промпт | ОК если |
|---|--------|---------|
| 16.1 | `Дай таблицу из двух колонок: язык | приветствие, для русского и английского.` | Таблица **рендерится** в UI |
| 16.2 | `Покажи пример цикла for на JavaScript в блоке кода.` | Подсветка синтаксиса или моноширинный блок |

---

## Если что-то «не ОК»

1. `docker compose logs gpthub-gateway --tail=80` — ошибки MWS, 401, таймауты.  
2. Совпадение id моделей с `GET /v1/models`: [MWS_TEAM_MODELS.md](MWS_TEAM_MODELS.md).  
3. Картинки в чате: `GPTHUB_PUBLIC_BASE_URL`, nginx `location /static/` → шлюз.  
4. Голос EN: `GPTHUB_ASR_DEFAULT_LANGUAGE` пусто для авто; `ru` только если нужен стабильный русский.
